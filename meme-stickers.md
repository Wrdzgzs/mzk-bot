# 🎨 Meme Stickers (表情包制作)

一站式制作 PJSK (Project Sekai) 样式表情包。

> **💡 提示**
> 下方参数说明中，部分数值支持以 `^` 开头，表示基于当前值的**相对偏移量**。

## ⚡ 快捷指令 (Shortcuts)

以下指令为常用预设，直接发送即可：

| 快捷指令 | 对应完整命令 | 示例 |
| :--- | :--- | :--- |
| **stickers** | `meme-stickers` | `stickers help` |
| **pjsk** | `meme-stickers generate pjsk` | `pjsk 角色名 文本` |
| **arcaea** / **arc** | `meme-stickers generate arcaea` | `arc 角色名 文本` |

---

## 🛠️ 生成指令详解

**基本语法：**
```bash
生成贴纸 [pack] [sticker] [text]
# 或者使用别名
gen [pack] [sticker] [text]
```
*   **pack**: 贴纸包 ID / 代号 / 名称
*   **sticker**: 贴纸 ID / 名称
*   **text**: 贴纸文本

---

## ⚙️ 详细参数选项 (Options)

在生成指令后附加以下参数进行微调。

| 参数 (简写) | 完整参数 | 功能说明 | 示例 |
| :--- | :--- | :--- | :--- |
| **-x** | `--assist-x` | **X轴坐标** (文本基线)<br>支持 `^` 开头表示相对偏移 | `-x 10`<br>`-x ^-5` |
| **-y** | `--assist-y` | **Y轴坐标** (文本基线)<br>支持 `^` 开头表示相对偏移 | `-y 20` |
| **-a** | `--align` | **对齐方式** (left/center/right) | `-a center` |
| **-r** | `--rotate` | **旋转角度**<br>支持 `^` 开头表示相对偏移 | `-r 45` |
| **-c** | `--color` | **文本颜色** (Hex 或 英文名) | `-c red` |
| **-C** | `--stroke-color` | **描边颜色** | `-C #000000` |
| **-W** | `--stroke-width-factor` | **描边宽度系数** | `-W 1.2` |
| **-s** | `--font-size` | **字体大小** | `-s 50` |
| **-S** | `--font-style` | **字体风格** (normal/italic等) | `-S italic` |
| **-A** | `--auto-resize` | **启用自动调整** (默认已开启)<br>当指定x或y时会自动禁用，需用此参强制开启 | `-A` |
| **-N** | `--no-auto-resize` | **禁用自动调整** | `-N` |
| **-f** | `--image-format` | **输出图片格式** (png/jpg/gif) | `-f png` |
| **-b** | `--background` | **背景颜色** (仅限 jpg/gif 类型) | `-b white` |
| **-D** | `--debug` | **启用调试模式** (输出Log) | `-D` |

---

## 🔧 资源管理 (仅限超级用户)

以下指令用于管理 Bot 的贴纸包资源。

### 1. 查看列表 (List)
*   `ls` / `list` / `ll`: 查看**本地**已安装的贴纸包
*   `ls -o` / `--online`: 查看**线上 Hub** 的贴纸包列表
*   `ls -a` / `--all`: 查看所有（包括不可用的）

### 2. 下载与安装 (Install)
*   `install` / `ins` / `download` / `add`: 下载贴纸包
    *   **用法**: `install [包名]`
    *   **示例**: `install pjsk`

### 3. 更新资源 (Update)
*   `update` / `up` / `upgrade`: 更新贴纸包
    *   **用法**: `update [包名]`
    *   **更新所有**: `update -a` / `--all`
    *   **强制更新**: `update -f` / `--force` (忽略本地版本)

### 4. 删除与维护 (Remove/Manage)
*   `remove` / `del` / `delete` / `rm`: 删除本地贴纸包
    *   **用法**: `rm [包名]`
    *   **跳过确认**: `rm [包名] -y`
*   `disable [包名]`: 禁用某个本地贴纸包
*   `enable [包名]`: 启用本地贴纸包
*   `reload`: 重新加载本地资源配置

---
*Generated by PicMenu-Next | Documented by mzk-bot Team*